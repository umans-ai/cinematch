# CineMatch Backend

FastAPI backend for CineMatch - swipe-based movie picker for couples.

## Development

```bash
# Install dependencies
uv sync

# Run dev server
uv run uvicorn app.main:app --reload

# Run tests
uv run pytest

# Run all checks
just check
```

See root `CLAUDE.md` for full conventions.
