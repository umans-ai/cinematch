dev:
    pnpm dev

build:
    pnpm build

start:
    pnpm start

lint:
    pnpm lint

typecheck:
    pnpm typecheck

check: lint typecheck

docker-build:
    docker build -t cinematch-frontend .

docker-run:
    docker run -p 3000:3000 cinematch-frontend
